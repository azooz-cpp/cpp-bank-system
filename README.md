A Simple Bank System:

What is the purpose of learning❓

- Developing programming logic
- Using and practicing the `while` loop in different ways
- Using the `break` loop to control when a specific condition is met
- Using the `i` and `j` counters
- Using conditions
- Using variables and initializing them with a value of 0
- Using variables appropriately when using `int.string` and `double`
- Using the `cin` function
- Checking input values ​​using `cin.fail()`
- Using `cin.clear();`
- Using `cin.ignore()`

................................................................................................................................

🙋🏻‍♂️:I know I haven't written the code perfectly, but I'm still learning, and I have no problem receiving advice from everyone as long as it aims to improve my skills or alert me to avoid mistakes. I would be happy to receive that.
